import React from 'react'
import { aboutText } from '../constants/applicationInfo'

const About = () => (
  <div className="container">
    <div className="about">
      <p>{aboutText}</p>
    </div>
  </div>
)

export default About
